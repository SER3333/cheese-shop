server {
    listen 80;
    server_name craftova-lavka.com;

    root /usr/share/nginx/html;
    index index.html;

    # React SPA
    location / {
        try_files $uri $uri/ /index.html;
    }

}
